# Shopify App Configuration
# Copy this file to .env.local and fill in your values

# Shopify API credentials (from Partner Dashboard)
SHOPIFY_API_KEY=your_api_key_here
SHOPIFY_API_SECRET=your_api_secret_here

# Your deployed application URL (must match the URL in shopify.app.toml)
SHOPIFY_APP_URL=https://your-app-domain.com

# Shopify Admin API access token (for custom store apps)
SHOPIFY_ADMIN_TOKEN=your_admin_access_token

# Shopify Storefront API access token
SHOPIFY_STOREFRONT_TOKEN=your_storefront_access_token

# OAuth scopes - should match the scopes in shopify.app.toml
SCOPES=read_files,write_files,read_metaobject_definitions,write_metaobject_definitions,read_metaobjects,write_metaobjects,read_online_store_navigation,write_online_store_navigation,write_online_store_pages,read_products,write_products,read_content,write_content,write_themes

# Next.js public environment variables (accessible in client-side code)
NEXT_PUBLIC_SHOPIFY_API_KEY=your_api_key_here

# Note: Never commit the actual .env file to version control!
# Keep your API secrets secure using environment variables in your hosting provider.
