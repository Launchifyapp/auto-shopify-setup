export async function runFullSetup({ shop, token }: { shop: string; token: string }) {
  console.log("Automatisation Shopify lancée pour :", shop);
  // Stub: tu pourras ajouter toute la logique réelle ici
  return;
}
